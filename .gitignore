# Keep Dockerfiles visible to the build
!**/Dockerfile

# Keep this file and .gitignore
!.dockerignore
!.gitignore

# Node + tooling noise
node_modules
npm-debug.log*
yarn.lock
pnpm-lock.yaml
.DS_Store
.git
.vscode
coverage
dist
build
.next
out
.tmp
.env
.env.*
